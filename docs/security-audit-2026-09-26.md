# VetMock cybersecurity review — 2026-09-26

Status: application remediation candidate 5.130.16 in `codex/security-0926`, rebased onto `9fb038d5` (5.130.15). Initial source review used `bdfc2a46`; all touched security implementation files were unchanged upstream, and the newer calendar fixtures and eval-free DICOM codecs were retained. Database protections SEC-04/05/06 and password reauthentication enforcement are live; application/cache patches are not deployed. This is a manual repository-wide source/runtime review with explicit limits below.

## Confirmed findings

| ID | Risk | Broken boundary | Remediation / evidence |
| --- | --- | --- | --- |
| SEC-01 | P1 | `study-coach` accepted inherited `constructor` as a mode, allowing caller `system`, `user`, `maxTokens` into a paid provider request before returning 500. | Own-property dispatch guard. New regression failed against baseline (500), then all 24 coach cases passed. Provider acceptance/billing was not probed. |
| SEC-02 | P2 | A playlist resolver debited daily quota once but performed up to ten Google requests. | Charge each upstream request, 8,000/day; preserve shelf bursts, pagination and RSS fallback. Five new handler checks failed against baseline and passed after the patch; combined focused quota suite 15/15. |
| SEC-03 | P2 | Restricted library bytes survived reload→sign-out; unsigned offline hash reads returned bytes, and late downloads recreated them. Unsigned cache labels also permitted content substitution. | Signed access classification, verified byte hash, live-client authorization and cache replacement. Focused tests 77/77; baseline fails 7/8 new checks. Native Chromium/Firefox/WebKit verify authorized offline reading, unsigned/logout denial, guest-tab coexistence and detached late writes. Chromium also verifies actual worker restart with a held URL. Personal PDF/ink storage preserved. |
| SEC-04 | P2 | `join_study_group` raised after incrementing the limiter, rolling failed guesses back. | Empty SETOF rows commit failed attempts and preserve old REST clients' error behavior. Transaction assertions prove 21 misses persist, correct codes are refused while exhausted and normal member joining resumes after reset. The shared frontend row reader also rejects all-null composites; regression failed before the guard and owning suite passed 8/8 afterward. |
| SEC-05 | P2 | Authenticated users could DELETE their own moderation reputation. Creating it again restored contributor defaults and removed a ban. | Revoke authenticated DELETE and drop the self-delete policy. Service-role account deletion remains permitted. Transaction assertions prove DELETE is denied, ensuring the row preserves the ban, and banned proposals fail. |
| SEC-06 | P2 | A NULL/missing reviewer role skipped SQL `NOT IN` denial and permitted unauthorized review writes. | Explicit NULL denial. Transaction assertions reject missing/NULL roles and preserve verified peer review. A new unauthorized row was excluded from peer tally; this is not evidence of founder takeover/publication. |

SQL migration: `supabase/migrations/20260926132348_security_boundaries_20260926.sql`. Runnable assertions: `tests/security/security-boundaries.sql`; all fixtures and mutations roll back. Candidate first passed inside a rollback transaction; original production definitions/grants were verified restored afterward. Independent review found no concrete SQL/API bypass or regression. Applied once through the migration API as version `20260926132348`, then reran the assertions against the committed live functions. Read-back confirms NULL guard, durable failed guesses, authenticated DELETE denied, service-role DELETE preserved, and zero fixture users/groups/submissions. The local filename was aligned to the actual returned ledger version; no SQL was reapplied or history repaired.

Follow-up `supabase/migrations/20260926140457_group_join_empty_result.sql` corrects the PostgreSQL/REST representation boundary: a NULL composite expands to an all-null record, which older clients might mistake for success. No dependent database objects/function callers were found. The RPC now returns zero rows on refusal and one real group row on success; original clients correctly treat `[]` as failure. Candidate and live post-apply assertions pass; anonymous EXECUTE remains denied and authenticated EXECUTE retained. Both migrations are additive; the first ledger entry was not edited.

## Production configuration and platform limits

- Password reauthentication enforcement was off while the existing UI already sent an email nonce. Enabled `security_update_password_require_reauthentication` with a one-field Management API PATCH; HTTP 200 and read-back true. Supabase requires recent sessions or reauthentication according to its [documented policy](https://supabase.com/docs/guides/auth/password-security); this does not mean every password change always requires an OTP. Actual OTP delivery/password-change UI was not exercised.
- Leaked-password protection remains off. A one-field enable request returned **402**: available on Pro and above, and read-back remained false. No plan purchase or password-policy substitution was made. [Supabase password security](https://supabase.com/docs/guides/auth/password-security).
- Live PostgreSQL reports 17.6. Supabase announced [17.11 security update](https://supabase.com/changelog/postgres-15-19-17-11-breaking-changes) on 25 September, including extension compatibility checks. Host upgrade/downtime has not been performed; this is a platform follow-up, not a proved exploitable application finding.
- Email autoconfirm is enabled; manual identity linking is disabled and secure email change is enabled. Changing the registration/LINE flow needs its own compatibility evidence and was not folded into these patches.

## Coverage and verified controls

- All 14 serverless routes and nine JavaScript API helpers reviewed: auth verification, signed receipts, origin/IP handling, shared quotas, provider fallbacks, storage gateways, MIME/header behavior. Shared limiter fails closed in production if configuration/backend is unavailable. No confirmed SSRF, forged bearer acceptance, header injection or server secret disclosure in that lane.
- Live schema/policy/function ACL snapshot and targeted function definitions reviewed. Owner RLS for user data/PDFs/study events; direct group-membership grants revoked; exam/race HMAC purpose and current-session checks; trusted default leaderboard excludes client-score receipts; admin readers enforce `is_admin()`; storage buckets private with published-case policies; profiles expose only intended public columns. Transaction assertions additionally exercise authenticated own-versus-other study/PDF reads, other-owner writes and ownership reassignment denial.
- Browser review covered imports, safe URL/render sinks, auth storage/account switches, local/cloud sync, passkeys and service-worker caching. No proved XSS in inspected dynamic HTML sinks. Custom video URL validation and device-local extra-data persistence are hardening/privacy considerations, not demonstrated account takeover.
- Tracked-file secret patterns over 4,437 tracked paths found documentation/public verification/test false positives only. Sensitive-file path history search found no matching commits; this was **not** an exhaustive all-history secret scan. CI workflows showed no privileged PR trigger or event-to-shell injection.
- Current release lockfile `npm audit`: **0 advisories**. Fresh isolated `npm ci --ignore-scripts` retained the current codec overrides and left the primary checkout's dependency tree intact. Advisory absence is not proof of code security. Initial focused suites passed (API lane 45, browser lane 85, SQL/source lane 46); these overlapping preexisting checks missed the confirmed gaps above.
- Deployed `line-auth` (including shared origin helper) and `delete-account` source matched local files after newline normalization. The two additional live temporary-import functions contain only permanent HTTP 410 tombstones; no import capability remains in those deployed endpoints.
- Low-impact production GET probes: shell 200; nonexistent catalogue slug 404; invalid blob token 403; unsupported write-route GETs 405. Security headers present. These probes do not prove authenticated application workflows or a new deployment.
- Live rejected-write probes: unknown cross-origin coach/feedback/action POSTs return 403; unsigned exam/race writes return 401 with private no-store. Inputs were chosen to fail before generation, email sending or signed database writes. Race's empty body returns validation 400 before auth; a syntactically valid body was used for the auth-denial assertion.

## Remaining verification / lower-confidence risks

- Independent patch review completed. Two cache regressions found during review (worker restart and guest-tab revocation) were corrected and covered by native browser tests. Final 5.130.16 candidate checks: data lint, build and three contrast audits pass; unit 1,826/1,826 in Bangkok and UTC with four unit workers. Both CI-parity E2E commands exited 0: Chromium reports 361 passed/1 flaky/19 skipped, WebKit+Firefox reports 355 passed/1 flaky/24 skipped. These are runner-reported totals, not a claim of zero first-attempt failures. Exact-SHA hosted CI/deployment/live application proof remains pending; no application push or archive-worker upload occurred.
- Race invite guessing has no per-account counter in `enter_race`; candidate abuse risk, not a demonstrated unauthorized join. LINE provider email guarantees, physical passkey behavior and HTTP cross-account denial beyond SQL assertions remain unverified.
- Rebuild drift: source Imaging owner-write policy differs from the stricter live published-state restrictions; no current production escalation was claimed.
- Library signed URLs intentionally remain bearer grants until expiry (up to 75 minutes on fixed mint windows); copied/downloaded files cannot be recalled. Cache changes close indefinite unsigned/revoked offline reads, not this documented temporary-link design. Existing HTTP cache entries from old releases expire naturally; the new restricted responses use no-store.
- Source evidence and intermediate results: local ignored `work/security-20260926/`, including live schema/advisors, auth config read-backs, mock reproducer and red/green test logs. No secret values or raw user records retained in the report. Actual OTP delivery/password change, physical passkeys, native PDF child range requests and exhaustive historical-secret scanning are not claimed verified.

## Concurrent-work handoff

The active bug lane is `fix/bughunt-0926` under the separate `vmu/fix-0926` checkout; it also edits `api/study-coach.js` for cache revision. Merge both narrow changes when assembling a release: security owns only the own-property mode guard, bug work owns the revision cache change. Main's existing `AGENTS.md`, curriculum edit and untracked files are preserved. Do not replace files wholesale or publish a stale baseline over the bug candidate.

The bug lane also edits `src/lib/api.js`; retain its changes alongside the security lane's three-line shared `rpcRow` normalization. The local candidate contains the two already-applied SQL ledger files and 5.130.16 package/changelog metadata. Only an additive handoff section was written in the primary checkout.

YouTube quota policy was checked against the [official quota calculator](https://developers.google.com/youtube/v3/determine_quota_cost): each `playlistItems.list`/`videos.list` page costs one unit; the default shared allocation for these endpoints is 10,000/day. This pass did not read the actual Google project's quota usage or increase provider quota.
